import { ClassNamesDirective } from './class-names.directive';

describe('ClassNamesDirective', () => {
  it('should create an instance', () => {
    const directive = new ClassNamesDirective();
    expect(directive).toBeTruthy();
  });
});
