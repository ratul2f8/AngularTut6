import { ClassWithParamsDirective } from './class-with-params.directive';

describe('ClassWithParamsDirective', () => {
  it('should create an instance', () => {
    const directive = new ClassWithParamsDirective();
    expect(directive).toBeTruthy();
  });
});
